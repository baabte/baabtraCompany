describe('<%= _.camelize(name) %>', function() {

	beforeEach(module('<%= appname %>'));

	it('should ...', inject(function($controller) {

        var controller = $controller('<%= _.camelize(name) %>');

		expect(controller('input')).toEqual('output');

	}));

});
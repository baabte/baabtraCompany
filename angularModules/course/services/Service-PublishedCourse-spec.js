describe('PublishedCourse', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(PublishedCourse) {

	//expect(PublishedCourse.doSomething()).toEqual('something');

  }));

});
describe('baabtraProfile', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(baabtraProfile) {

	//expect(baabtraProfile.doSomething()).toEqual('something');

  }));

});
describe('batchAttendance', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(batchAttendance) {

	//expect(batchAttendance.doSomething()).toEqual('something');

  }));

});
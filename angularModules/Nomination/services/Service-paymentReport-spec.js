describe('paymentReport', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(paymentReport) {

	//expect(paymentReport.doSomething()).toEqual('something');

  }));

});
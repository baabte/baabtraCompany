describe('globalSettings', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(globalSettings) {

	//expect(globalSettings.doSomething()).toEqual('something');

  }));

});
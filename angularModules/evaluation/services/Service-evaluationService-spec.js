describe('evaluationService', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(evaluationService) {

	//expect(evaluationService.doSomething()).toEqual('something');

  }));

});
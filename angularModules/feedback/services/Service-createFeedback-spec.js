describe('createFeedback', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(createFeedback) {

	//expect(createFeedback.doSomething()).toEqual('something');

  }));

});
describe('questionAnswering', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(questionAnswering) {

	//expect(questionAnswering.doSomething()).toEqual('something');

  }));

});
describe('attendenceService', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(attendenceService) {

	//expect(attendenceService.doSomething()).toEqual('something');

  }));

});
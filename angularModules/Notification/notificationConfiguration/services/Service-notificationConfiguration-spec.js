describe('notificationConfiguration', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(notificationConfiguration) {

	//expect(notificationConfiguration.doSomething()).toEqual('something');

  }));

});
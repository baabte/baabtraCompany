describe('bulkEnrollment', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(bulkEnrollment) {

	//expect(bulkEnrollment.doSomething()).toEqual('something');

  }));

});
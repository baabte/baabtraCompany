describe('courseAllocateService', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(courseAllocateService) {

	//expect(courseAllocateService.doSomething()).toEqual('something');

  }));

});
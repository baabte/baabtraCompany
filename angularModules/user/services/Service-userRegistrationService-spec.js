describe('userRegistrationService', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(userRegistrationService) {

	//expect(userRegistrationService.doSomething()).toEqual('something');

  }));

});
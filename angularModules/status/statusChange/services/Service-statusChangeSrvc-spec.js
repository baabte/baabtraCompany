describe('statusChangeSrvc', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(statusChangeSrvc) {

	//expect(statusChangeSrvc.doSomething()).toEqual('something');

  }));

});
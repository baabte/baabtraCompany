describe('emailSmsConfig', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(emailSmsConfig) {

	//expect(emailSmsConfig.doSomething()).toEqual('something');

  }));

});